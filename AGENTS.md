# RedNoteBot Project Rules

本项目的开发与运行方式约定如下：

## 1. 项目定位

- 本项目是一个基于 Python、`uv` 和 Playwright 的小红书自动发布工具。
- 当前仓库只聚焦“小红书长文模式”这一条主链路，不以覆盖图文、视频、直播或移动端流程为目标。
- 项目的核心输入是 `notes/` 目录下的 Markdown 文件，核心输出是驱动小红书创作中心完成发布。
- 项目追求“可维护、可观察、可人工介入”的自动化，而不是完全黑盒、零人工参与的强对抗脚本。

## 2. 整体设计思路

- 解析层负责把 Markdown 笔记转换为结构化内容，发布层负责浏览器自动化，两者职责分离。
- 浏览器自动化只面向小红书网页版创作中心，不直接调用未封装的私有接口作为主流程。
- 页面流程采用“场景识别 + 动作选择器”的结构：
  - 场景识别用于判断当前页面处于登录页、创作平台页、长文入口页、长文编辑页或发布结果页。
  - 动作选择器用于在对应场景中执行登录、进入长文、填写内容、上传图片、排版和发布。
- 选择器与场景检测规则集中维护，优先修改集中定义，不鼓励把页面选择器散落到各处流程代码中。
- 当页面结构变化时，优先更新场景判定和选择器，不要轻易推翻整条发布链路。

## 3. 开发环境

- 本地开发环境为 Windows。
- 开发、调试、手动登录、页面联调时，应优先在 Windows 本机运行项目。
- 本地开发允许直接调用系统浏览器或 Playwright 启动的可视化浏览器，便于人工观察页面和验证流程。
- 如需执行浏览器相关测试或排查登录问题，统一在 Windows 环境完成。

## 4. 运行环境边界

- 本项目当前不维护 Docker 运行方式，也不提供容器化部署约定。
- 发布执行、手动登录、选择器调试、页面特征采集均以 Windows 本机运行为准。
- 如无用户明确要求，不要新增 Dockerfile、Compose、容器启动脚本或容器专用运行参数。

## 5. 登录态设计原则

- 登录态的标准载体是 Playwright `storage_state` 文件，默认文件名为 `playwright-state.json`。
- 首次登录、登录失效后的重新登录、人工扫码验证，都应在 Windows 本机有界面浏览器中完成。
- 如需长期运行，应将 `playwright-state.json` 视为需要周期性刷新和替换的运行凭证。
- `playwright-state.json` 本质上属于敏感登录凭证，不得提交到仓库，不得写入公开文档示例中的真实内容，不得泄露到日志或截图中。

## 6. 本地运行职责

- Windows 本地环境负责：
  - 首次登录
  - 登录失效后的重新认证
  - 选择器调试
  - 页面特征采集
  - 浏览器可视化联调
- Markdown 输入消费与发布执行也在 Windows 本地完成。
- 不要引入仅为容器化运行服务的额外流程、依赖或抽象，除非用户明确要求改变项目方向。

## 7. 自动化边界与失败处理

- 本项目默认接受“半自动”现实：登录、风控、扫码、验证码、账号异常等问题允许人工介入。
- 如果脚本检测到停留在登录页，默认含义应是“需要人工在本地完成登录”，而不是继续尝试绕过验证。
- 对于页面结构变化、按钮失效、上传失败、跳转异常等问题，优先增强日志、截图、页面特征采集和选择器维护能力。
- 不应默认把失败归因于网络或偶发现象；应优先确认页面场景是否变化、选择器是否失效、登录态是否过期。
- 不应为了追求“全自动”而增加明显脆弱或高风险的绕过逻辑。

## 8. 代码组织原则

- CLI 入口负责参数解析和调用，不承担页面流程细节。
- Markdown 解析逻辑应保持独立，避免与 Playwright 页面操作代码耦合。
- 发布流程应尽量保持线性可读：打开页面、确认登录、进入编辑页、填写表单、提交发布。
- 新增能力时，优先判断它属于：
  - Markdown 解析能力扩展
  - 浏览器流程扩展
  - 页面识别与选择器维护
  - 运行方式与文档补充
- 若新增功能会明显扩大产品边界，例如支持新的内容类型或新的站点入口，应在文档和规则中明确，而不是隐式扩张。

## 9. 文档与命令约束

- 涉及本地调试说明时，默认提供 Windows 用法。
- 涉及运行、部署或批量执行说明时，默认提供 Windows 用法，不再补充 Docker 用法。
- 涉及首次登录说明时，默认明确写出“Windows 本地有界面登录并保存状态文件”。
- 涉及运行命令、README、脚本默认参数时，必须保证与上述职责边界一致。
- 若命令示例依赖 `playwright-state.json`，应同时提醒该文件为敏感凭证且需要宿主机持久化保存。

## 10. 执行约束

- 修改运行方式、启动命令、文档或脚本时，必须保持“Windows 本地运行”这一主假设一致。
- 任何可能改变项目整体方向的改动，都应先检查是否破坏了以下主假设：
  - Windows 负责首次登录和调试
  - Windows 负责实际发布执行
  - Playwright 状态文件是登录态复用的桥梁
  - 当前主链路只服务于小红书长文发布
- 如无用户明确要求，不要把项目改造成：
  - 依赖 Docker 的运行方案
  - 脱离 Markdown 输入的全新内容生产工具
  - 面向多平台、多内容类型的大而全发布系统
